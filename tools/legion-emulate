#!/usr/bin/env python3
"""Press the buttons of a Legion Go from here. See emulator/cli.py."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from emulator.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
