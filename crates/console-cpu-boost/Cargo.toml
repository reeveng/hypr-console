[package]
name = "console-cpu-boost"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# Reading a file without turning a fault into an absence, and writing the note
# whole. Both of those are one decision this workspace makes in one place.
console-file-writing.workspace = true
console-never.workspace = true

[lib]
name = "console_cpu_boost"
path = "src/lib.rs"

[lints]
workspace = true
