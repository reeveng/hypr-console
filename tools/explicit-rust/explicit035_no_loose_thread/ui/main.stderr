error: this thread's handle is dropped on the line that made it, so nothing can ask whether it is still running and nothing says whether outliving its starter was meant
  --> $DIR/main.rs:8:5
   |
LL | /     let _ = std::thread::spawn(move || {
LL | |         let _ = 1_u32.saturating_add(1);
LL | |     });
   | |_______^
   |
   = help: hold the handle and `join` it where the thread ends, or say that it was let go: `console_program_lifetime::threads::let_go` is the same drop with a word on it, and it is the argument `Alongside` and `LetGo` already make about a child
   = note: `#[deny(explicit035_no_loose_thread)]` on by default

error: this thread's handle is dropped on the line that made it, so nothing can ask whether it is still running and nothing says whether outliving its starter was meant
  --> $DIR/main.rs:20:5
   |
LL |     let _ = named();
   |     ^^^^^^^^^^^^^^^^
   |
   = help: hold the handle and `join` it where the thread ends, or say that it was let go: `console_program_lifetime::threads::let_go` is the same drop with a word on it, and it is the argument `Alongside` and `LetGo` already make about a child

error: aborting due to 2 previous errors

