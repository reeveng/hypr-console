# Who the greeter runs as: nobody, with the screen and the pad. logind grants
# both to the greeter's session while it is the one on the seat; the groups are
# what is left if it does not.
u console-greeter - "Login screen" / -
m console-greeter video
m console-greeter input
