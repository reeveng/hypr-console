[package]
name = "console-virtual-touchscreen"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[[bin]]
name = "console-tap"
path = "src/bin/tap.rs"

[dependencies]
console-never.workspace = true
console-number-conversion.workspace = true
console-screen.workspace = true
# The device this makes. `console-gamepad` builds the emulator's pad with it and
# `console-controller` publishes its wheel with it, so it is already on the
# handheld; this used to write the same ioctls by hand beside them. `tap.rs`
# says why that stopped.
evdev = "0.13"

[lints]
workspace = true
