[Unit]
Description=Restore and keep saving the open windows
PartOf=legion.target

[Service]
Type=simple
# Mode is positional. Passed as an option it saves somewhere nothing reads.
ExecStart=/usr/local/bin/hyprsession
Restart=always
RestartSec=5

[Install]
WantedBy=legion.target
